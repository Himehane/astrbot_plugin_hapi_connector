{
  "hapi_endpoint": {
    "description": "HAPI 服务地址",
    "type": "string",
    "default": "",
    "hint": "例如: http://localhost:3006"
  },
  "access_token": {
    "description": "HAPI Access Token (支持 token:namespace 格式)",
    "type": "string",
    "default": ""
  },
  "proxy_url": {
    "description": "代理地址 (支持 socks5h:// 和 http://)",
    "type": "string",
    "default": "",
    "hint": "例如: socks5h://127.0.0.1:1080 或 http://127.0.0.1:7890"
  },
  "cf_access_client_id": {
    "description": "Cloudflare Zero Trust Access Client ID（留空则不启用 CF 认证）",
    "type": "string",
    "default": "",
    "hint": "部署在 Cloudflare Access 后面时需要填写，可在 Zero Trust 控制台创建 Service Token 获取"
  },
  "cf_access_client_secret": {
    "description": "Cloudflare Zero Trust Access Client Secret",
    "type": "string",
    "default": "",
    "hint": "与 Client ID 配对使用"
  },
  "max_reconnect_attempts": {
    "description": "SSE 最大重连次数（0 表示无限重连）",
    "type": "int",
    "default": 10,
    "hint": "达到上限后停止自动重连，节约资源。发送 /hapi list 可重新唤醒"
  },
  "jwt_lifetime": {
    "description": "JWT 有效期 (秒)",
    "type": "int",
    "default": 900
  },
  "refresh_before_expiry": {
    "description": "JWT 提前刷新时间 (秒)",
    "type": "int",
    "default": 180
  },
  "output_level": {
    "description": "SSE 推送级别",
    "type": "string",
    "default": "simple",
    "options": ["silence", "simple", "summary", "detail"],
    "hint": "silence: 仅推送权限请求；simple: 推送纯文本消息及系统事件；summary: 任务完成时推送最近 N 条消息；detail: 实时推送所有消息"
  },
  "summary_msg_count": {
    "description": "summary 推送级别显示的 agent 消息条数",
    "type": "int",
    "default": 5
  },
  "quick_prefix": {
    "description": "快捷发送前缀",
    "type": "string",
    "default": ">",
    "hint": "前缀后需加空格，如「> 消息内容」发送到当前会话，「>2 消息内容」发送到第 2 个会话"
  },
  "poke_approve": {
    "description": "启用戳一戳快捷操作",
    "type": "bool",
    "default": true,
    "hint": "仅 QQ NapCat 等支持戳一戳的适配器。关闭后戳机器人无任何 hapi 动作"
  },
  "poke_action": {
    "description": "戳一戳映射动作",
    "type": "string",
    "default": "approve",
    "options": [
      "approve",
      "pending",
      "list",
      "status",
      "stop",
      "output_cycle",
      "none"
    ],
    "hint": "approve=批准待审（默认）；pending/list/status=查看；stop=中止当前 session；output_cycle=循环切换推送级别；none=仅确认。不提供一戳拒绝（误触成本高，请用 /hapi deny）。可在 WebUI「交互优化」选择"
  },
  "remind_pending": {
    "description": "待审批请求超时重复提醒",
    "type": "bool",
    "default": true,
    "hint": "防止 AI 会话缓存失效"
  },
  "remind_interval": {
    "description": "待审批重复提醒间隔（秒）",
    "type": "int",
    "default": 180
  },
  "auto_approve_enabled": {
    "description": "忙时托管审批：在指定时间范围内自动批准所有权限请求（默认关闭）",
    "type": "bool",
    "default": false,
    "hint": "⚠️ 请谨慎开启，启用后 AI 将在指定时间段内无需确认自动执行所有操作"
  },
  "auto_approve_start": {
    "description": "忙时托管审批开始时间（HH:MM，24小时制）",
    "type": "string",
    "default": "23:00"
  },
  "auto_approve_end": {
    "description": "忙时托管审批结束时间（HH:MM，24小时制，可跨午夜）",
    "type": "string",
    "default": "07:00",
    "hint": "例如开始 22:00、结束 08:00，即晚上10点到次日早上8点，支持跨午夜"
  },
  "default_notification_window": {
    "description": "默认通知发送窗口",
    "type": "string",
    "default": "",
    "hint": "留空则发送到 session 绑定的窗口，填写窗口 ID 则强制发送到指定窗口"
  },
  "render_mode": {
    "description": "推送呈现模式",
    "type": "string",
    "default": "text",
    "options": ["text", "auto", "card"],
    "hint": "text=全部纯文本（最快）；auto=list/pending 等结构信息出卡片，Agent 对话仍文本；card=更多结构类型出卡。卡片需要可选依赖 Pillow（pip install -r requirements-render.txt），未安装时自动回退文本"
  },
  "formula_mode": {
    "description": "公式渲染策略（预留）",
    "type": "string",
    "default": "off",
    "options": ["off", "detect", "always"],
    "hint": "首版仅预留配置；开启后复杂公式仍以源码文本回退。后续可接可选数学引擎"
  },
  "render_kinds": {
    "description": "允许出卡的消息类型（逗号分隔）",
    "type": "string",
    "default": "session_list,pending,status,permission",
    "hint": "可选：session_list,pending,status,permission,routes。仅 render_mode 为 auto/card 时生效"
  },
  "card_style_preset": {
    "description": "推送卡片样式预设",
    "type": "string",
    "default": "terminal_light",
    "options": ["terminal_light", "terminal_dark", "clean", "compact"],
    "hint": "可在 WebUI「交互优化 → 推送呈现」里预览与微调"
  },
  "card_width": {
    "description": "推送卡片宽度（像素）",
    "type": "int",
    "default": 720,
    "hint": "范围 400–1200；手机场景可试 560"
  },
  "card_accent": {
    "description": "卡片强调色",
    "type": "string",
    "default": "#1a7f4b",
    "hint": "十六进制颜色，如 #1a7f4b"
  },
  "card_bg": {
    "description": "卡片背景色",
    "type": "string",
    "default": "#faf8f2"
  },
  "card_fg": {
    "description": "卡片主文字色",
    "type": "string",
    "default": "#1c1914"
  },
  "card_font_scale": {
    "description": "卡片字号倍率（百分数，100=1.0）",
    "type": "int",
    "default": 100,
    "hint": "75–150"
  },
  "card_density": {
    "description": "卡片密度",
    "type": "string",
    "default": "comfortable",
    "options": ["comfortable", "compact"]
  },
  "card_show_brand": {
    "description": "卡片显示品牌角标",
    "type": "bool",
    "default": true
  },
  "card_mono": {
    "description": "卡片优先等宽字体",
    "type": "bool",
    "default": true
  }
}

