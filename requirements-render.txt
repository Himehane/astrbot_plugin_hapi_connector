# 可选：推送卡片出图（WebUI 实卡预览 / render_mode=auto|card / 对话 message 卡）
# 不安装时插件仍可正常运行，渲染回退纯文本。
#
#   pip install -r requirements-render.txt
#
# 基础（Pillow，结构卡 + 对话卡 + CSS 变量子集）
Pillow>=10.0,<12

# 公式内嵌（Agent 消息 formula_mode=detect 时用）
# matplotlib>=3.7,<4

# 完整自定义 CSS 保真（推荐，可选）
# playwright>=1.40
# 安装后还需：playwright install chromium
