# 可选：推送卡片出图（WebUI 实卡预览 / render_mode=card / 对话 message 卡）
# 不安装时插件仍可正常运行，渲染回退纯文本。
#
#   pip install -r requirements-render.txt
#
# 基础（Pillow，结构卡 + 无公式对话卡）
Pillow>=10.0,<12

# 公式（formula_mode=detect / formula_only：公式用 matplotlib 渲染）
# matplotlib>=3.7,<4
